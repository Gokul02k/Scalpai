"""Strategy core, ported from `app/lib/*.js`.

Kept behaviourally identical to v1 and pinned there by the parity tests in
`engine/tests`. Improvements to the strategy are welcome but belong in their
own commits, measured against the backtest — otherwise a port bug and a
deliberate change become indistinguishable.
"""
