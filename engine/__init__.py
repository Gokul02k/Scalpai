"""ScalpAI trading engine (v2).

Owns market data, indicators, signals, backtesting and — later — execution.
The Next.js app in `app/` becomes a read-only view over this.
"""

__version__ = "2.0.0-dev"
